<analysis>**original_problem_statement:**
The user initially reported a could not load RDS settings error. After this was resolved, a cascade of new issues and feature requests emerged. The main goals were to fix bugs related to show scheduling, data isolation between sites, and WordPress integration, while also adding new features like multi-site user assignment and making the RDS monitor optional.

During this session, several of these were resolved, but a major new feature was requested: syncing the Clara application's calendar with a ProRadio-powered schedule on two WordPress sites (mfy.be and grk.fm). After a lengthy and complex attempt to integrate directly with ProRadio's internal data structures (which use ACF), the user and agent agreed to pivot to a more robust, decoupled solution: creating a brand new, custom WordPress plugin that will pull schedule data from a new public API endpoint in the Clara backend, effectively replacing the ProRadio schedule viewer.

PRODUCT REQUIREMENTS:
1.  **Bugfix:** Mini-site password updates must work for network admins. (DONE)
2.  **UI:** The Network Admin dashboard header should be styled like the main site dashboards, with the title Clara Global. (DONE)
3.  **Bugfix:** Enabled features for a Main Site must be saved correctly. (DONE)
4.  **Feature:** The Clara calendar must be displayed on the WordPress sites (mfy.be, grk.fm), replacing the existing ProRadio schedule display. This involves creating a custom WordPress plugin and a new public API endpoint in the Clara backend.

**User's preferred language**: Dutch. The next agent MUST respond in Dutch.

**what currently exists?**
The application is largely functional. The agent successfully resolved three key issues in this session: a critical authorization bug preventing network admins from updating mini-site passwords, a UI request to update the network admin header, and a bug where main site feature settings weren't being saved.

The agent is now in the middle of a major new feature: creating a full-fledged WordPress plugin to display the Clara schedule. The previous approach of syncing directly with the existing ProRadio plugin was abandoned due to its complexity and brittleness. The agent has already written the PHP and CSS code for the new WordPress plugin and is about to create the required public API endpoint in the Clara backend to serve the schedule data.

**Last working item**:
-   **Last item agent was working:** The user requested a new, custom WordPress plugin to handle both displaying the schedule and syncing with Clara, abandoning the complex ProRadio integration. The agent provided the complete PHP and CSS code for this new plugin and confirmed with the user that the next step is to create the public API endpoint in the Clara backend that the plugin will consume.
-   **Status:** IN PROGRESS
-   **Agent Testing Done:** N
-   **Which testing method agent to use?** This is a multi-step integration task.
    1.  **Backend:** After creating the public API endpoint, test it with  to ensure it returns the correct schedule data without authentication (but respecting the  header).
    2.  **Integration:** Guide the user to create the new plugin files ( and ) on their WordPress instances, configure the plugin with the new Clara API endpoint URL, and use the plugin's built-in Test Connection feature.
    3.  **Frontend:** Once the integration is confirmed, verify the final result by having the user add the  shortcode to a WordPress page and visually checking if the schedule appears correctly.
-   **User Testing Done:** N

**All Pending/In progress Issue list**:
*   None. All reported bugs have been resolved. The current work is a new feature implementation.

**In progress Task List**:
*   **Task 1: Implement ProRadio Sync via Custom WordPress Plugin and Public Clara API (P0 - HIGHEST)**
    *   **Where to resume:**
        1.  **Clara Backend:** Create a new, public, unauthenticated API endpoint. A new file like  would be appropriate. The endpoint (e.g., ) should accept a  parameter (, , ) and require an  header for data scoping. It must return the schedule data (show title, start/end times, etc.) for the specified station(s) in a JSON format that the new WordPress plugin expects.
        2.  **WordPress Frontend:** Guide the user to create two new files in their WordPress plugins directory ():
            *    (code already provided in chat history)
            *    (code already provided in chat history)
        3.  **Cleanup:** Once the new system is verified and working, remove the now-obsolete ProRadio integration code from the Clara backend (, , and the background sync tasks in ).
    *   **What will be achieved with this?** A robust, real-time sync and display of the Clara schedule on the WordPress websites, replacing the problematic ProRadio integration with a custom-built, maintainable solution.
    *   **Status:** IN PROGRESS
    *   **Should Test frontend/backend/both after fix?** Both
    *   **Blocked on something:** The Clara backend public API endpoint must be created first.

**Upcoming and Future Tasks**
*   **Upcoming Tasks:**
    *   **(P1) Configurable Stale Now Playing Timeout:** Implement a UI and API to allow users to configure the 15-minute hardcoded timeout currently in .
*   **Future Tasks:**
    *   **(P2) Site Cloning/Copying:** Implement the user's request to be able to copy/clone Main and Mini sites.
    *   **(P2) Stream Monitor VU Meters:** Implement functional VU meters on the stream monitor page.

**Completed work in this session**
-   **Mini-site Password Update (RESOLVED):** Fixed the Site niet gevonden authorization bug in  that prevented network admins from setting mini-site passwords.
-   **Network Admin Header UI (DONE):** Modified  to display a Clara Global header consistent with the main site dashboard style.
-   **Main Site Feature Toggles (RESOLVED):** Fixed a bug where main site feature settings were not being saved by adding the  field to the  model in .

**Earlier issues found/mentioned but not fixed**
*   None.

**Known issue recurrence from previous fork**
*   **Data Scoping (team_id vs main_site_id):** This pattern remains a risk. The mini-site password bug was another symptom of incorrect data scoping logic. Future data-related endpoints must prioritize .
*   **WordPress/ACF Integration Complexity:** A new key learning from this session. Direct integration with third-party WordPress plugins that use ACF without a clean REST API is extremely brittle. The pivot to a decoupled approach (public API + custom plugin) is the correct pattern to avoid this in the future.

**Code Architecture**
enabled_features

**Key Technical Concepts**
-   **API-First Decoupling:** When a 3rd-party integration is too complex (like ProRadio/ACF), the best practice is to pivot. Create a stable, public API on the source system (Clara) and build a simple consumer for it on the target system (a new WordPress plugin). This creates a clean contract and avoids brittle dependencies.
-   **WordPress Plugin Development:** The agent created a complete WordPress plugin, demonstrating knowledge of admin pages, settings API, REST API endpoints, shortcodes, and asset enqueuing.
-   **WordPress Transients API:** Used  and  for server-side caching of API responses within the WordPress plugin, reducing load on the Clara backend.

**key DB schema**
-   No changes to the MongoDB schema in this session.

**changes in tech stack**
-   None.

**All files of reference**
-   : Contains the fix for the mini-site password update.
-   : Contains the updated Network Admin header UI.
-   : Contains the fix for the main site feature toggles bug.
-   , , : These files contain the logic for the **abandoned** ProRadio sync attempt and should be cleaned up once the new solution is implemented.

**Areas that need refactoring**:
-   Once the new public API and custom WordPress plugin are verified, the obsolete ProRadio sync code must be removed. This includes the service (), the router (), its registration in , and all background task calls within .

**key api endpoints**
-   : **FIXED**.
-   **TO BE CREATED:** : New unauthenticated endpoint in the Clara backend for the WordPress plugin to consume.
-   **NEW (in WordPress):** The custom plugin will create its own endpoints, including  and  to act as a drop-in replacement for any themes relying on the old ProRadio API structure.

**Critical Info for New Agent**
-   **Language:** The user is Dutch. All communication MUST be in Dutch.
-   **Top Priority:** Your first task is to build the backend component of the new schedule sync solution. Create the public API endpoint () in the Clara application.
-   **New Approach:** Do not try to fix the old ProRadio integration. The agreed-upon plan is to use a new custom WordPress plugin that consumes a new public API from Clara. The user already has the code for the plugin.
-   **Guidance:** After creating the backend endpoint, you will need to guide the user on how to create the plugin files ( and ) on their WordPress server and configure it.
-   **Cleanup:** After the new feature is working, remember to plan for the cleanup of the old, unused ProRadio integration code in the backend.

**documents and test reports created in this job**
-    was read and updated.

**Last 10 User Messages and any pending HUMAN messages**
1.  **User:** Requests a new WordPress plugin to be created that handles both the schedule view and the sync with Clara.
2.  **Agent:** Agrees and provides the full PHP code for the new plugin () and the accompanying CSS.
3.  **User:** Asks if they can replace the old plugin code with the new one and tells the agent to create the necessary backend endpoint.
4.  **Agent:** Confirms they can, and states they will create the backend endpoint first and then provide complete instructions.

**Project Health Check:**
-   **Broken:**
    -   None.
-   **Mocked:**
    -   None.

**3rd Party Integrations**
-   **WordPress:** The integration is being rebuilt to be more robust via a custom plugin and a public API, decoupling it from the ProRadio theme's internal implementation.
-   **ffmpeg:** System dependency for audio triggers.

**Testing status**
-   **Testing agent used after significant changes:** NO
-   **Troubleshoot agent used after agent stuck in loop:** NO
-   **Test files created:** None
-   **Known regressions:** None

**Credentials to test flow:**
-   **Network Admin:** 
-   **Password:** 

**What agent forgot to execute**
-   The agent has not yet created the promised public API endpoint in the Clara backend (), which is the critical next step required to unblock the user and complete the new feature.</analysis>
